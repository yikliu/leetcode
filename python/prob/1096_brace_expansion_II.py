class BraceExpansionII:

    def brace_expansion_ii(self, expression: str) -> List[str]:
      
        def _read_string(self, expression, i):
            res = []
            while i < len(expression) and expression[i].isalpha():
                i += 1
                res.append(expression[

        def parse(expr, i):

            cur = set()
            while i < len(expr):
                ch = expr[i]
                if ch.isalpha():

